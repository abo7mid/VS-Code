
    //var tall;
    //var age;

function check_Child_eligibility_by_both_height_and_age(tall,age )
{
  //this.tall = tall;
  //this.age= age;
    if(tall > 52 && age > 11 )
    console.log("Get on that ride, kiddo!")

    else
    console.log("Sorry kiddo. Maybe next year")
}

function check_Child_eligibility_by_either_height_or_age(tall,age)
{

    if(tall > 52 || age > 11 )
    console.log("Get on that ride, kiddo!")

    else
    console.log("Sorry kiddo. Maybe next year")
}

check_Child_eligibility_by_both_height_and_age(51,12);
check_Child_eligibility_by_either_height_or_age(51,12);


index = 1;
index = index  + 1;
index++
++index
index += 1;
console.log(index)

index = index  - 1;
index--
--index
index -= 1;
console.log(index)
